import { addtoCarrito, carrito, loadFromStorage } from "../../data/carrito.js";

describe('testiando el addtoCarrito',()=>{
  it('add un producto existente', ()=>{
  
  });
  it('add un nuevo producto', ()=>{
   
  });
});